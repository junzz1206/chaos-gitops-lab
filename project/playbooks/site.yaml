---
# ─────────────────────────────────────────────
# site.yaml — 전체 오케스트레이션 (최소 구조, 실전용)
# ─────────────────────────────────────────────

- name: ⚙️ Full Automation Pipeline
  hosts: all
  become: yes

  tasks:
    # ─────────────────────────────────────────────
    # 1️⃣ 앱 배포 (K3s 클러스터 내부)
    # ─────────────────────────────────────────────
    - name: 🚀 Deploy Application
      include_role:
        name: deploy

    # ─────────────────────────────────────────────
    # 2️⃣ 모니터링 스택 (Prometheus + Grafana + Loki + Promtail)
    # ─────────────────────────────────────────────
    - name: 📊 Setup Prometheus + Grafana
      include_role:
        name: monitoring

    - name: 🧾 Setup Loki + Promtail
      include_role:
        name: loki

    # ─────────────────────────────────────────────
    # 3️⃣ 카오스 엔지니어링 실험 (CPU, Network, Process)
    # ─────────────────────────────────────────────
    - name: 💣 Run Chaos Experiments
      include_role:
        name: chaos_cpu
      vars:
        duration: 30
        cores: 2

    - name: 🌐 Apply Network Chaos
      include_role:
        name: chaos_network
      vars:
        delay_ms: 250
        duration: 30

    - name: 💀 Kill Target Process
      include_role:
        name: chaos_kill
      vars:
        target_process: "nginx"
        duration: 30

    # ─────────────────────────────────────────────
    # 4️⃣ 자동 복구 (AlertManager 트리거 or 수동 복구)
    # ─────────────────────────────────────────────
    - name: 🔁 Auto Recovery Routine
      include_role:
        name: recovery
      vars:
        service_name: "nginx"

